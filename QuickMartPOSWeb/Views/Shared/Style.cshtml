<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

<style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #fff8f0;
        color: #3b2f2f;
        margin: 0;
        padding: 20px;
    }

    h2 {
        text-align: center;
        font-size: 2.2rem;
        font-weight: 700;
        color: #6a4f4b;
        margin-bottom: 30px;
        letter-spacing: 1px;
        text-transform: uppercase;
        font-family: 'Courier New', Courier, monospace;
        margin-top:20px;
    }

    .receipt-table {
        width: 100%;
        border-collapse: separate;
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 6px 18px rgba(106, 79, 75, 0.1);
        font-family: 'Courier New', Courier, monospace;
    }

        .receipt-table th,
        .receipt-table td {
            padding: 14px 18px;
            font-size: 1rem;
            text-align: left;
            color: #4a3a38;
            border-bottom: 1px solid #e3d9d7;
            word-wrap: break-word;
            border-right: 1px solid #c49b82;
        }

    table.receipt-table td:last-child {
        border-right: none;
    }

    table.receipt-table th {
        background-color: #6a4f4b;
        background-image: linear-gradient(135deg, #6a4f4b 0%, #4e3a38 100%) !important;
        color: #f9f1ef !important;
        font-weight: 700;
        letter-spacing: 1.2px;
        text-transform: uppercase;
        border-bottom: 3px solid #c49b82 !important;
        background-image: none !important;

    }


        .receipt-table tbody tr:nth-child(even) {
            background-color: #f9f4f1;
        }

        .receipt-table tbody tr:hover {
            background-color: #e6dbd7;
        }

        .receipt-table tr:last-child td {
            border-bottom: none;
            font-weight: 600;
            color: #6a4f4b;
        }

    .summary {
        max-width: 500px;
        margin: 30px auto;
        padding: 25px 30px;
        background: #fff7ee;
        border: 1px solid #c49b82;
        border-radius: 10px;
        font-family: 'Courier New', Courier, monospace;
        color: #5b4636;
        box-shadow: 0 4px 12px rgba(196, 155, 130, 0.15);
    }

        .summary p {
            font-size: 1.1rem;
            font-weight: 600;
            margin: 8px 0;
            display: flex;
            justify-content: space-between;
            border-bottom: 1px dashed #c49b82;
            padding-bottom: 6px;
        }

            .summary p:last-child {
                border-bottom: none;
                font-size: 1.3rem;
                color: #b15e29;
                font-weight: 700;
            }

            .summary p span {
                min-width: 100px;
                text-align: right;
            }

    .actions {
        margin-top: 40px;
        text-align: center;
    }

    .btn {
        display: inline-block;
        padding: 14px 28px;
        font-size: 1rem;
        border-radius: 8px;
        text-decoration: none;
        cursor: pointer;
        border: none;
        transition: all 0.3s ease;
        margin: 10px 5px;
        box-shadow: 0 3px 8px rgba(177, 94, 41, 0.2);
        user-select: none;
    }

    .btn-primary {
        background-color: #b15e29;
        color: white;
    }

        .btn-primary:hover {
            background-color: #7d3f12;
            box-shadow: 0 5px 15px rgba(125, 63, 18, 0.5);
        }

    .btn-secondary {
        background-color: #6a4f4b;
        color: white;
    }

        .btn-secondary:hover {
            background-color: #4e3a38;
            box-shadow: 0 5px 15px rgba(78, 58, 56, 0.5);
        }

    .footer {
        margin-top: 60px;
        text-align: center;
        font-size: 1rem;
        color: #6a4f4b;
        font-style: italic;
        font-family: 'Courier New', Courier, monospace;
        user-select: none;
    }

    /* === Print Styles === */
    @@media print {
        body

    {
        background-color: white;
        padding: 0;
        color: black;
        font-size: 12pt;
        font-family: 'Courier New', Courier, monospace;
    }

    .btn,
    .actions,
    h2 {
        display: none !important;
    }

    .footer {
        display: block !important;
        text-align: center;
        font-size: 20px;
        margin-top: 30px;
        font-family: 'Courier New', Courier, monospace;
        color: black !important;
        font-style: italic;
    }


    p {
        text-align: left !important;
        font-size: 14px;
        margin: 4px 0;
        color: black !important;
        font-weight: normal;
    }

    .summary {
        box-shadow: none;
        border: none;
        max-width: 100%;
        margin: 0;
        padding: 0;
    }

    }
    @@media print {
        .receipt-table

    {
        box-shadow: none;
        border: 1px solid black;
        border-radius: 0;
    }

    .receipt-table th {
        background-color: black !important;
        color: white !important;
        border-bottom: 2px solid black !important;
    }

    .receipt-table td {
        border-bottom: 1px solid black !important;
    }

    }

    @@media (max-width: 600px) {
        .receipt-table th, .receipt-table td

    {
        padding: 10px 6px;
        font-size: 0.9rem;
    }

    }

    /* === Mobile Responsive === */
    @@media (max-width: 600px) {
        body

    {
        padding: 10px;
        font-size: 14px;
    }

    h2 {
        font-size: 1.6rem;
        margin-bottom: 20px;
    }

    .table th,
    .table td {
        padding: 10px 6px;
        font-size: 0.9rem;
    }

    .summary {
        max-width: 100%;
        padding: 15px;
        font-size: 0.95rem;
    }

        .summary p {
            flex-direction: column;
            align-items: flex-start;
        }

            .summary p span {
                text-align: left;
                margin-top: 4px;
                font-weight: 700;
            }

    .btn {
        width: 100%;
        margin: 8px 0;
        padding: 12px 0;
        font-size: 1rem;
    }

    .footer {
        font-size: 0.9rem;
        margin-top: 40px;
        padding: 0 10px;
    }

    }
</style>


<style>
   
    .form-wrapper {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100%;
    }

    .product-form {
        width: 90%;
        max-width: 700px;
        background-color: #f4f7fa;
        padding: 30px 40px;
        border-radius: 10px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .form-title {
        text-align: center;
        font-size: 36px;
        font-weight: 800;
        font-family: 'Poppins', sans-serif;
        color: #0a192f;
        margin-bottom: 25px;
    }

    .form-group {
        margin-bottom: 20px;
    }

        .form-group label {
            display: block;
            font-weight: 600;
            margin-bottom: 8px;
            color: #333;
        }

    .category-label {
        font-size: 18px;
        color: #1a237e;
    }

    .form-control {
        width: 100%;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 6px;
        transition: border-color 0.3s ease;
    }

        .form-control:focus {
            border-color: #00bfa5;
            outline: none;
        }

    .btn-submit {
        background-color: #28a745;
        color: white;
        border: none;
        padding: 14px;
        font-size: 17px;
        border-radius: 6px;
        cursor: pointer;
        width: 100%;
        margin-top: 10px;
        transition: background-color 0.3s ease;
    }

        .btn-submit:hover {
            background-color: #218838;
        }

    .hidden {
        display: none;
    }

</style>
@*----------- POSInterface------------------------- *@
<style>
    
    .container {
        max-width: 1100px;
        background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15), 0 8px 24px rgba(0, 0, 0, 0.1);
        border-radius: 20px;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        color: #2c3e50;
        position: relative;
        overflow: hidden;
    }

        .container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="0.5" fill="rgba(41,120,181,0.02)"/><circle cx="75" cy="75" r="0.5" fill="rgba(41,120,181,0.02)"/><circle cx="50" cy="10" r="0.3" fill="rgba(41,120,181,0.01)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            pointer-events: none;
            z-index: 0;
        }

        .container > * {
            position: relative;
            z-index: 1;
        }

    .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: linear-gradient(135deg, #2978b5 0%, #1e5f8a 100%);
        color: white;
        padding: 30px;
        margin: -40px -35px 35px -35px;
        border-radius: 20px 20px 0 0;
        box-shadow: 0 8px 32px rgba(41, 120, 181, 0.3);
        flex-wrap: nowrap;
        flex-shrink: 0;
        position: relative;
        overflow: hidden;
    }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, transparent 30%, rgba(255,255,255,0.1) 50%, transparent 70%);
            transform: translateX(-100%);
            animation: shimmer 3s infinite;
        }

    @@keyframes shimmer {
        0%

    {
        transform: translateX(-100%);
    }

    100% {
        transform: translateX(100%);
    }

    }

    .logo {
        display: flex;
        align-items: center;
        gap: 15px;
        padding-left:30px;
        padding-top:50px;
   }

    .logo-icon {
        width: 150px;
        height: 150px;
        background: linear-gradient(45deg, #ff6b6b, #feca57);
        border-radius: 30%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 28px;
        font-weight: bold;
        color: white;
        box-shadow: 0 10px 30px rgba(255, 107, 107, 0.3);
        animation: pulse 2s infinite;
    }

    @@keyframes pulse {
        0%, 100%

    {
        transform: scale(1);
    }

    50% {
        transform: scale(1.05);
    }

    }

    .store-info {
        text-align: center;
        flex: 1 1 100%;
        margin: 0 20px;
        margin-top: 20px;
    }

        .store-info h1 {
            margin: 0 0 8px 0;
            font-weight: 700;
            font-size: 2.5rem;
            color: white;
            text-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            background: linear-gradient(45deg, #ffffff, #e8f4f8);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .store-info p {
            margin: 4px 0;
            font-size: 18px;
            color: rgba(255, 255, 255, 0.9);
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
        }

    .user-info {
        background: rgba(255, 255, 255, 0.15);
        padding: 10px;
        border-radius: 15px;
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        font-size: 0.95rem;
        font-weight: 500;
        color: white;
        min-width: 200px;
        margin-top: 20px;
        margin-right: 20px;
    }

    .info-row {
        display: grid;
        grid-template-columns: 70px auto;
        gap: 8px;
        margin-bottom: 8px;
    }

    .label {
        font-weight: 700;
        opacity: 0.9;
    }

    .categories {
        display: flex;
        gap: 12px;
        margin-bottom: 30px;
        flex-wrap: wrap;
        justify-content: center;
        padding: 20px 0;
    }

    .category {
        background: linear-gradient(135deg, #2978b5 0%, #1e5f8a 100%);
        color: white;
        padding: 12px 20px;
        border-radius: 30px;
        cursor: pointer;
        font-weight: 700;
        font-size: 1rem;
        box-shadow: 0 6px 20px rgba(41, 120, 181, 0.3);
        transition: all 0.3s ease;
        user-select: none;
        position: relative;
        overflow: hidden;
        border: 2px solid transparent;
    }

        .category::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .category:hover::before {
            left: 100%;
        }

        .category:hover,
        .category.active {
            background: linear-gradient(135deg, #1e5f8a 0%, #145068 100%);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 10px 30px rgba(41, 120, 181, 0.4);
            border-color: rgba(255, 255, 255, 0.3);
        }

    .table-container {
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(10px);
        border-radius: 15px;
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.3);
        margin-bottom: 30px;
    }

    table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
        background: transparent;
    }

    th, td {
        padding: 16px 20px;
        text-align: left;
        font-size: 1.5rem;
        font
        border-bottom: 1px solid rgba(41, 120, 181, 0.1);
    }

    th {
        background: linear-gradient(135deg, #2978b5 0%, #1e5f8a 100%);
        color: white;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
    }

    tbody tr {
        transition: all 0.3s ease;
    }

        tbody tr:hover {
            background: linear-gradient(135deg, rgba(41, 120, 181, 0.1) 0%, rgba(30, 95, 138, 0.05) 100%);
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(41, 120, 181, 0.1);
        }

        tbody tr:nth-child(even) {
            background: rgba(248, 249, 250, 0.5);
        }

    td input[type="number"] {
        width: 70px;
        padding: 8px 12px;
        border: 2px solid rgba(41, 120, 181, 0.2);
        border-radius: 8px;
        font-size: 1rem;
        transition: all 0.3s ease;
        background: rgba(255, 255, 255, 0.9);
    }

        td input[type="number"]:focus {
            border-color: #2978b5;
            outline: none;
            box-shadow: 0 0 0 3px rgba(41, 120, 181, 0.1);
            transform: scale(1.02);
        }

    .btn-add-cart {
        background: linear-gradient(135deg, #FF7043 0%, #ff5722 100%);
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 25px;
        cursor: pointer;
        font-weight: 600;
        font-size: 0.95rem;
        box-shadow: 0 6px 20px rgba(255, 112, 67, 0.3);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

        .btn-add-cart::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .btn-add-cart:hover::before {
            left: 100%;
        }

        .btn-add-cart:hover {
            background: linear-gradient(135deg, #ff5722 0%, #e64a19 100%);
            transform: translateY(-2px) scale(1.05);
            box-shadow: 0 10px 30px rgba(255, 112, 67, 0.4);
        }

        .btn-add-cart:active {
            transform: translateY(-1px) scale(0.98);
        }

    .cart {
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(248, 249, 250, 0.9) 100%);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 30px;
        margin-top: 30px;
        border: 1px solid rgba(255, 255, 255, 0.3);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    }

        .cart h3 {
            margin-bottom: 20px;
            font-size: 1.8rem;
            font-weight: 700;
            color: #1b4f72;
            text-align: center;
            background: linear-gradient(135deg, #2978b5 0%, #1e5f8a 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

    .totals {
        background: rgba(41, 120, 181, 0.05);
        padding: 20px;
        border-radius: 15px;
        border: 1px solid rgba(41, 120, 181, 0.1);
        margin: 20px 0;
    }

        .totals p {
            font-size: 1.2rem;
            font-weight: 600;
            margin: 10px 0;
            color: #2c3e50;
            text-align: left;
        }

    .form-group {
        margin: 25px 0;
    }

        .form-group label {
            font-size: 1.1rem;
            font-weight: 600;
            display: block;
            margin-bottom: 10px;
            color: #1b4f72;
        }

        .form-group input[type="number"] {
            width: 100%;
            max-width: 300px;
            padding: 12px 16px;
            font-size: 1rem;
            border-radius: 10px;
            border: 2px solid rgba(41, 120, 181, 0.3);
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.9);
        }

            .form-group input[type="number"]:focus {
                outline: none;
                border-color: #2978b5;
                box-shadow: 0 0 0 3px rgba(41, 120, 181, 0.1);
                transform: scale(1.02);
            }

    button.btn-success {
        background: linear-gradient(135deg, #2978b5 0%, #1e5f8a 100%);
        color: white;
        padding: 16px 32px;
        border: none;
        border-radius: 50px;
        cursor: pointer;
        font-size: 1.2rem;
        font-weight: 700;
        box-shadow: 0 8px 25px rgba(41, 120, 181, 0.4);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

        button.btn-success::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        button.btn-success:hover::before {
            left: 100%;
        }

        button.btn-success:hover {
            background: linear-gradient(135deg, #1e5f8a 0%, #145068 100%);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 12px 35px rgba(41, 120, 181, 0.5);
        }

    .actions {
        display: flex;
        gap: 15px;
        justify-content: center;
        margin-top: 30px;
        flex-wrap: wrap;
    }

        .actions button,
        .actions a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            padding: 14px 28px;
            border-radius: 25px;
            border: none;
            cursor: pointer;
            font-weight: 700;
            font-size: 1rem;
            transition: all 0.3s ease;
            text-decoration: none;
            white-space: nowrap;
            position: relative;
            overflow: hidden;
        }

        .actions button {
            background: linear-gradient(135deg, #c0392b 0%, #922b21 100%);
            color: white;
            box-shadow: 0 6px 20px rgba(192, 57, 43, 0.3);
        }

        .actions a.btn-primary {
            background: linear-gradient(135deg, #2980b9 0%, #1c5980 100%);
            color: white;
            box-shadow: 0 6px 20px rgba(41, 128, 185, 0.3);
        }

        .actions button::before,
        .actions a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .actions button:hover::before,
        .actions a:hover::before {
            left: 100%;
        }

        .actions button:hover {
            background: linear-gradient(135deg, #922b21 0%, #7a1f1a 100%);
            transform: translateY(-2px) scale(1.05);
            box-shadow: 0 10px 30px rgba(192, 57, 43, 0.4);
        }

        .actions a.btn-primary:hover {
            background: linear-gradient(135deg, #1c5980 0%, #154360 100%);
            transform: translateY(-2px) scale(1.05);
            box-shadow: 0 10px 30px rgba(41, 128, 185, 0.4);
        }

    @@media (max-width: 900px) {
        .header

    {
        flex-direction: column;
        gap: 20px;
        text-align: center;
        padding: 30px 20px;
    }

    .store-info {
        order: 2;
        margin: 0;
    }

    .logo {
        order: 1;
        justify-content: center;
    }

    .user-info {
        order: 3;
        min-width: auto;
        width: 100%;
        max-width: 300px;
    }

    }


    @@media (max-width: 600px) {
        .container

    {
        border-radius: 15px;
    }

    .header {
        margin: -25px -20px 25px -20px;
        border-radius: 15px 15px 0 0;
    }

    .categories {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 10px;
        justify-items: center;
        padding: 15px 0;
    }

    .category {
        font-size: 0.85rem;
        padding: 10px 16px;
        width: 100%;
        text-align: center;
        border-radius: 20px;
    }

    #product-table,
    #product-table thead,
    #product-table tbody,
    #product-table th,
    #product-table td {
        font-size: 10px;
        padding: 8px;
    }

        #product-table td input[type="number"] {
            width: 50px;
            padding: 6px;
        }

    .btn-add-cart {
        font-size: 0.8rem;
        padding: 8px 16px;
    }

    .actions {
        flex-direction: column;
        gap: 12px;
    }

        .btn:not(:disabled):not(.disabled) {
            cursor: pointer;
            width: 58%;
        }

        .actions button,
        .actions a {
            width: 50%;
            justify-content: center;
        }

    .store-info h1 {
        font-size: 2rem;
    }

    }
    @@media (max-width: 600px) {
        .cart

    {
        padding: 20px 15px;
        border-radius: 15px;
        margin-top: 20px;
    }

    .cart h3 {
        font-size: 1.4rem;
    }

    #cart-items {
        width: 100%;
        table-layout: fixed;
        font-size: 0.85rem;
    }

        #cart-items th,
        #cart-items td {
            padding: 8px 6px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            font-size: 12px;
        }

    }
    
</style>